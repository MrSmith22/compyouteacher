/**
 * Canonical assignment identity strings.
 * Single source of truth — must match existing Supabase rows.
 */
export const MLK_ASSIGNMENT_ID = "mlk-rhetorical-analysis" as const;
export const MLK_ASSIGNMENT_NAME = "MLK Essay Assignment" as const;

/** Default assignment for the current product (MLK template). */
export const DEFAULT_ASSIGNMENT_ID = MLK_ASSIGNMENT_ID;
export const DEFAULT_ASSIGNMENT_NAME = MLK_ASSIGNMENT_NAME;
